/**
 * ** POC ** Liste des clés de remotes consommés par le legacy-shell.
 * Le mapping URL est désormais dans `src/assets/federation.manifest.json`
 * et chargé par initFederation() au démarrage.
 */
export type MfeRemoteKey = 'mfeStats' | 'mfeNotifications'
